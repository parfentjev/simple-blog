export interface PostDto {
    id: string
    title: string
    summary: string
    text: string
    date: string
    visible: boolean
}

export interface PostPreviewDto {
    id: string
    title: string
    summary: string
    date: string
    visible: boolean
}
