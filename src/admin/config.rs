use actix_web::web::ServiceConfig;

// use crate::admin::controllers::{post_user, post_user_token};

pub fn admin_config(config: &mut ServiceConfig) {
    // config
    //     .service(post_user)
    //     .service(post_user_token);
}